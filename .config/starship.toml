# $schema: https://starship.rs/config-schema.json

# A minimal left prompt
#format = """$character"""

# Wait up to 10ms for starship to check files under the current directory
#scan_timeout = 100

# move the rest of the prompt to the right
#right_format = """$all"""

# Disable the blank line at the start of the prompt
#add_newline = false

[gcloud]
disabled = true

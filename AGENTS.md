# Agent instructions

This repo uses [Agent Skills](https://agentskills.io). Skills live in `.agents/skills/` so any coding agent can load them.

When adding a file or folder to these dotfiles, follow `.agents/skills/add-dotfile/SKILL.md`.
